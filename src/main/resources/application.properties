spring.application.name=users

# Puerto donde corre este microservicio
server.port=8081

# Conexión a PostgreSQL en Supabase
spring.datasource.url=jdbc:postgresql://db.kuzcttaoofjiwztbztgi.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=GrupoAVM152410
# Driver de PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect